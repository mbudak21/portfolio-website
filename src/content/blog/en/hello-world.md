---
title: "Hello, World"
description: "Why this blog exists, and what lands here."
pubDate: 2026-05-30
tags: ["meta", "notes"]
---

Every site eventually grows a blog. This one is mostly a place to dump notes I'd otherwise lose in a `~/scratch.md` that gets `rm`'d during a cleanup spree.

## What goes here

Short write-ups on the things I build:

- systems, and the occasional `wayland` config that nukes itself
- graphics experiments that look better than they perform
- whatever I had to debug at 3am

## A code block, because we have to

```ts
export const greet = (name: string) => `hello, ${name}`;
```

> If it compiles, ship it. If it doesn't, blame the cache.

That's the whole pitch. Delete this post when the real ones show up.
